export interface MdlProps {
  show?: boolean;
  persistent?: boolean;
  showBtn?: boolean;
  Header?: string;
  Subheader?: string;
  mobile?: boolean;
}

export interface LogRegCarouselProps {
  persistent?: boolean;
  Header?: string;
  Subheader?: string;
  setOpen: any;
}
